package psywerx.tanks

import javax.media.opengl.GL

object Game {
  val program = Main.program
  val gl = program.gl
  init()
  
  def init() = {
    
  }
  def tick(theta:Double) = {
    
  }
  def draw() = {
    
  }
}